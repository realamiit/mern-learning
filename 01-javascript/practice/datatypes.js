// ====DATA TYPE IN JAVASCRIPT =====

// 1. STRING : Text Data hota hai - quots (" ") me likhte hai 
const questionName  = "Two Sum";
const topic = "Array";
const difficulty = "Easy";

// 2. NUMBER : intiger and decimal dono eek hi me store hoge 
const totalQuestions = 150;
const accuracy = 85.5;

// 3. BOOLEAN : sirf 2 value true/false
const isSolved = true;
const isBookMarked = false;

// 4. NULL : intentionally empty rakha hai — developer ne khud set kiya
const assignedData = null;

// 5. UNDEFINED : value abhi tak assign hi nahi hui
// const lastRevisionDate; // it gave error because of const constant hota hai aur hme iski value define krni hi hogi thats why
let lastRevisionDate;


// 6. typeof : variable ka type check karta hai
console.log(typeof questionName);
console.log(typeof topic);
console.log(typeof difficulty);
console.log(typeof totalQuestions);
console.log(typeof accuracy);
console.log(typeof isSolved);
console.log(typeof isBookMarked);
console.log(typeof assignedData);
console.log(typeof lastRevisionDate)

